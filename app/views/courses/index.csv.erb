<%=
response.content_type = 'application/courses'
CSV.generate do |csv|
	csv << ["id", "facility_id", "name"]
	@courses.each do |course|
    csv << [ course[:id], course[:facility_id], course[:name] ]
  end
end .html_safe
%>