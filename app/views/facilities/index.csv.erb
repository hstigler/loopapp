<%=
response.content_type = 'application/facilities'
CSV.generate do |csv|
	csv << ["id", "identifier", "name"]
	@facilities.each do |facility|
    csv << [ facility[:id], facility[:identifier], facility[:name] ]
  end
end .html_safe
%>