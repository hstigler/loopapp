<%=
response.content_type = 'application/holes'
CSV.generate do |csv|
	csv << ["id", "course_id", "number", "par", "handicap"]
	@holes.each do |hole|
    csv << [ hole[:id], hole[:course_id], hole[:number], hole[:par], hole[:handicap]]
  end
end .html_safe
%>