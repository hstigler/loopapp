<%=
response.content_type = 'application/players'
CSV.generate do |csv|
	csv << ["id", "identifier", "firstName", "lastName", "handicap"]
	@players.each do |player|
    csv << [ player[:id], player[:identifier], player[:firstName], player[:lastName], player[:handicap]]
  end
end .html_safe
%>