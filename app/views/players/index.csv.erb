<%=
response.content_type = 'application/players'
CSV.generate do |csv|
	csv << ["id", "firstName", "lastName", "handicap"]
	@players.each do |player|
    csv << [ player[:id], player[:firstName], player[:lastName], player[:handicap]]
  end
end .html_safe
%>