<%=
response.content_type = 'application/round_holes'
CSV.generate do |csv|
	csv << ["id", "round_id", "hole_id"]
	@round_holes.each do |round_hole|
    csv << [ round_hole[:id], round_hole[:round_id], round_hole[:hole_id]]
  end
end .html_safe
%>