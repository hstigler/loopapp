<%=
response.content_type = 'application/tees'
CSV.generate do |csv|
	csv << ["id", "course_id" "name", "distance", "courseRating", "slopeRating"]
	@tees.each do |tee|
    csv << [ tee[:id], tee[:course_id], tee[:name], tee[:distance], tee[:courseRating], tee[:slopeRating]]
  end
end .html_safe
%>